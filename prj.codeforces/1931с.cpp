#include <iostream>
#include <vector>

int main(){
    int t = 0;
    std::cin >> t;
    while (t > 0){
        int p = 0;
        std::cin >> p;
        std::vector<int> arr;
        while(p > 0){
            int temp;
            std::cin >> temp;
            arr.push_back(temp);
        }
    }
}